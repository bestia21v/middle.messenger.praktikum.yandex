<div class="change-password">
    <div class="overlay">
        {{>subTitle}}
        <form class="form change-password__form">
            {{#each fields}}
                {{>field}}
            {{/each}}
            {{>button save}}
        </form>
    </div>
</div>